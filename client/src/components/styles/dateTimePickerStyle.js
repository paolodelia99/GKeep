import { createMuiTheme } from "@material-ui/core";
import amber from "@material-ui/core/colors/amber";

export const dateTimePickerStyle = createMuiTheme({
    palette: {
        primary: amber,
    },
});